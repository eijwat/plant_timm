# TIMM-JSXによる画像分類器の作成プロジェクト

TIMMファインチューニングによる画像分類ツール
少数画像（各クラス30〜100枚程度）から転移学習で分類モデルを構築できる。  
昆虫・植物など任意の分類タスクに使い回せる。

## ファイル構成

.
├── scripts/           # 学習＋推論 Gradio UIなどコードの保存先
├── images/           # 画像データの保存先
├── saved_models/    # 学習済みモデルの自動保存先
├── docs/    # 環境ファイルやプロジェクトの進行ログなど
├── venv/    # venvファイル
├── results/    # 結果の出力先
└── CLAUDE.md        # このファイル

## 作るアプリ

UIアプリでは
１）各クラスの画像フォルダーを指定（クラスのデフォルト名称はフォルダー名だが、指定もできる）
２）使用するTIMMモデルを指定（ConvNeXtとEfficientNetの大小モデル）
３）その後、RUNすると転移学習をしてモデルをつくる
４）推論タブへ行き、推論する画像フォルダーを指定するとクラス推定を行う。

## デモ

デモンストレーション用に植物の葉の画像データセットをダウンロードして使用する

## 環境

CUDA 利用可。venv環境。
必要なpythonライブラリーは自分でインストール。
apt系のインストールはエイジさんに要求。
必要なフォルダーやファイルは自分で作成。

## 進め方

Claudeができるだけ自律的に進める。
エイジさんにはできるだけ分かりやすく会話する。